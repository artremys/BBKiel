package com.example.bbkiel.localDatabase;

import android.content.Context;

import androidx.room.Room;

import dagger.Module;
import dagger.Provides;
import dagger.hilt.InstallIn;
import dagger.hilt.android.qualifiers.ApplicationContext;
import dagger.hilt.components.SingletonComponent;
//import com.example.bbkiel.model.UserDao;

import javax.inject.Singleton;

//@Module
//@InstallIn(SingletonComponent.class)
public class UserDatabaseModule {
/*
    @Singleton
    @Provides
    public UserDatabase provideDatabase(@ApplicationContext Context appContext) {
        return Room.databaseBuilder(appContext, UserDatabase.class, "Local-Database").build();
                // .addTypeConverter(new Converter()).build();
    }

    @Singleton
    @Provides
    public UserDao provideUserDao(UserDatabase database) {
        return database.getUserDao();
    }

 */


}
